use std::process::Command;

use common::*;

#[test]
fn piping() {
    let p = setup(&[tf("bla")]);
//    let output = Command::new(PATH).output().expect("123");

//    assert_eq!("", String::from_utf8_lossy(&output.stdout));
//    assert_eq!("", String::from_utf8_lossy(&output.stderr));
}